---
phase: 04-app-ecosystem
plan: 02
subsystem: configs
tags: [nvim, lazyvim, ghostty, htop, gh, kanata, chezmoi]

# Dependency graph
requires:
  - phase: 04-01
    provides: Package installation infrastructure (packages.yaml, run_onchange scripts)
provides:
  - nvim/LazyVim configuration with all customizations
  - ghostty terminal configuration
  - htop process viewer settings
  - gh CLI configuration (excluding OAuth tokens)
  - kanata keyboard remapping (macOS only)
  - Updated .chezmoiignore with app-specific exclusions
affects: [05-finalize]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Static config files (no .tmpl needed for app configs)
    - Conditional ignore via .chezmoiignore for platform-specific configs
    - Secrets exclusion pattern (hosts.yml never managed)

key-files:
  created:
    - home/dot_config/nvim/ (22 files)
    - home/dot_config/ghostty/config
    - home/dot_config/htop/htoprc
    - home/dot_config/gh/config.yml
    - home/dot_config/kanata/kanata.kbd
  modified:
    - home/.chezmoiignore

key-decisions:
  - "nvim lazy-lock.json excluded (auto-generated by lazy.nvim)"
  - "gh hosts.yml excluded (contains OAuth tokens)"
  - "kanata config included but ignored on non-darwin via .chezmoiignore"
  - "lazygit paths added to ignore for future plan"

patterns-established:
  - "App configs copied as static files (no templating needed)"
  - "Runtime artifacts listed in .chezmoiignore"
  - "Secrets files explicitly excluded from management"

# Metrics
duration: 2min
completed: 2026-01-19
---

# Phase 04 Plan 02: App Configs Summary

**LazyVim, Ghostty, htop, gh CLI, and Kanata configs migrated to chezmoi with proper runtime/secret exclusions**

## Performance

- **Duration:** 2 min
- **Started:** 2026-01-19T03:04:17Z
- **Completed:** 2026-01-19T03:06:49Z
- **Tasks:** 3
- **Files created:** 26

## Accomplishments

- Full nvim/LazyVim configuration with 13 custom plugin files
- Terminal config (Ghostty) and system tools (htop, gh CLI)
- Kanata keyboard remapping for macOS devices
- Comprehensive .chezmoiignore updates for runtime artifacts and secrets

## Task Commits

Each task was committed atomically:

1. **Task 1: Copy nvim/LazyVim configuration** - `097668d` (feat)
2. **Task 2: Copy remaining application configs** - `38371fa` (feat)
3. **Task 3: Update .chezmoiignore for new configs** - `ebef748` (chore)

## Files Created/Modified

- `home/dot_config/nvim/` - Full LazyVim setup (init.lua, lazy.lua, 13 plugins)
- `home/dot_config/ghostty/config` - Terminal emulator configuration
- `home/dot_config/htop/htoprc` - Process viewer settings
- `home/dot_config/gh/config.yml` - GitHub CLI settings (editor, aliases)
- `home/dot_config/kanata/kanata.kbd` - Keyboard remapping (macOS)
- `home/.chezmoiignore` - Added nvim artifacts, gh secrets, lazygit paths

## Decisions Made

- **nvim lazy-lock.json excluded:** Auto-generated by lazy.nvim on plugin install; would cause constant drift
- **gh hosts.yml excluded:** Contains OAuth tokens (secrets) - never version controlled
- **Kanata via .chezmoiignore:** Already in darwin conditional from Phase 3; no changes needed
- **Lazygit paths pre-added:** Different locations on macOS (Library) vs Linux (.config)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All major application configs now managed
- Phase 4 complete - ready for Phase 5 (Finalize)
- User will need to migrate chezmoi sourceDir to use .dotfiles repo

---
*Phase: 04-app-ecosystem*
*Completed: 2026-01-19*
